const styles = theme => ({
  progress: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: '15px',
  },
  user: {
    cursor: 'pointer',
  },
  error: {
    marginTop: '15px'
  }
});

export default styles;
